spring.application.name=sanguidaily-back

# Active profile (dev/prod). Override via SPRING_PROFILES_ACTIVE.
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

# Database (set via environment variables)
spring.datasource.username=root
spring.datasource.password=333
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Jackson settings
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# WeChat login
wechat.appid=wx830bf7ba0912e6be
wechat.secret=e061c08ccb3980d4bcdcd36289b7758a
#app.owner-openid=o08Iz6CR3cN4u1J9v9521h9nF_sE
app.owner-openid=oOi9B3fkCGP1C5DqJt-UdEOHzkPU

# JWT
app.jwt.secret=THIS_IS_SANGUI_DAILY_PROJECT_JWT_SECRET_HELLOOOOO_WORRRRRLD
app.jwt.expire-days=30

# CORS (comma-separated origins)
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://sangui.top,https://www.sangui.top}

# Upload root

# Video cover (ffmpeg)

# Upload size limits
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB
